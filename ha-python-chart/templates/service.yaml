apiVersion: v1
kind: Service
metadata:
  name: {{ include "ha-python-chart.fullname" . }}
  labels:
    {{- include "ha-python-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ha-python-chart.selectorLabels" . | nindent 4 }}
