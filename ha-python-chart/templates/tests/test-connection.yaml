apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ha-python-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ha-python-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ha-python-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
